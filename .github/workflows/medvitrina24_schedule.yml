name: Schedule Scripts

on:
  schedule:
    - cron: '0 13 * * *'   # Запуск в 13:00 UTC (для мед витрины)
    - cron: '0 1 * * *'    # Запуск в 01:00 UTC (для мед витрины)
    - cron: '0 14 * * *'   # Запуск в 14:00 UTC (для витрины)
    - cron: '0 2 * * *'    # Запуск в 02:00 UTC (для витрины)

jobs:
  run-medvitrina24-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '20.x'  # Обновленная версия Node.js

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'  # Укажите актуальную версию Python

      # Добавьте дополнительные шаги для выполнения скрипта med.vitrina24 здесь

  run-vitrina24-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '20.x'  # Обновленная версия Node.js

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'  # Укажите актуальную версию Python

      # Добавьте дополнительные шаги для выполнения скрипта vitrina24 здесь
